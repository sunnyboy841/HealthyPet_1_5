<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- 页面标题 -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="铲屎论坛"
        android:textSize="24sp"
        android:textStyle="bold"
        android:layout_marginBottom="20dp"/>

    <!-- 帖子列表 -->
    <ListView
        android:id="@+id/postListView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"/>

    <!-- 添加帖子按钮 -->
    <Button
        android:id="@+id/addPostButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="发布新帖子"
        android:layout_marginTop="16dp"/>

    <!-- 示例内容：帖子列表项（可保留或删除，根据实际需求） -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="12dp"
        android:orientation="vertical"
        android:layout_marginBottom="16dp"
        android:background="@drawable/ic_post_bg"
        android:visibility="gone"> <!-- 设置为gone隐藏示例内容，避免冲突 -->

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="如何训练狗狗定点排便？"
            android:textSize="18sp"
            android:textStyle="bold"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="楼主：铲屎官小明 | 浏览量：1254"
            android:textSize="12sp"
            android:layout_marginTop="8dp"
            android:textColor="@color/gray"/>  <!-- 次要信息灰色 -->
    </LinearLayout>

    <!-- 其他帖子项可继续添加（建议隐藏或移除，避免布局冲突） -->
</LinearLayout>